# Docker Environment Configuration Template
# Copy this to .env before running docker-compose

# PostgreSQL Configuration
POSTGRES_USER=wms
POSTGRES_PASSWORD=wms_secure_password_123
POSTGRES_DB=wms

# DATABASE_URL for Docker
# CRITICAL: Use "db" as the hostname (matches service name in docker-compose.yml)
# This is NOT "localhost" - Docker containers use service names to communicate
DATABASE_URL="postgresql://wms:wms_secure_password_123@db:5432/wms?schema=public"

# NextAuth Configuration
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="REPLACE_WITH_OUTPUT_FROM_OPENSSL_COMMAND"
NEXTAUTH_URL="http://localhost:3000"

# Company Information (for ZPL Labels)
NEXT_PUBLIC_COMPANY_NAME="Fresh Farm Logic LLC"
NEXT_PUBLIC_COMPANY_ADDRESS="1234 Produce Lane, Salinas, CA"

# Application Configuration
NODE_ENV="production"

# QuickBooks Online Integration
QBO_CLIENT_ID="your_qbo_client_id"
QBO_CLIENT_SECRET="your_qbo_client_secret"
QBO_REDIRECT_URI="http://localhost:3000/api/auth/qbo/callback"
QBO_ENVIRONMENT="sandbox"  # or "production"

# QuickBooks Auto-Sync Configuration
# Set to "true" to enable background auto-sync service
QBO_AUTO_SYNC_ENABLED="false"
# Sync interval in minutes (1, 5, 10, 15, 30, or 60)
QBO_SYNC_INTERVAL_MINUTES="1"
# Enable/disable specific sync types
QBO_SYNC_CUSTOMERS="true"
QBO_SYNC_PRODUCTS="true"
QBO_SYNC_VENDORS="true"
QBO_SYNC_INVOICES="true"

# Email (Google Workspace SMTP)
# Use an App Password for EMAIL_PASSWORD (2FA required)
SALES_EMAIL="sales@srjlabs.com"
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT="465" # use 587 for STARTTLS
EMAIL_USER="sales@srjlabs.com"
EMAIL_PASSWORD="REPLACE_WITH_APP_PASSWORD"
EMAIL_FROM="sales@srjlabs.com"

# Cron Job Security
# Generate with: openssl rand -base64 32
# Used to secure /api/cron/* endpoints
CRON_SECRET="REPLACE_WITH_SECRET_TOKEN"

